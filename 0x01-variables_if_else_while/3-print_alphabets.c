#include <stdio.h>
/**
 * main -Entry point 
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char l;

	char h;

	l = 'a';
		h = 'A';
		while 
			(l <= 'z') {
				putchar(l);
				l++;
			}
	while 
		(h <= 'Z') {
			putchar(h);
			h++;
		}
	putchar('\n');
	return (0);
}

