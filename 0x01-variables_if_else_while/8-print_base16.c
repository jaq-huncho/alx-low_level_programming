#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char p;

	int i;

	p = 'a';
	i = 0;
	while
		(i < 10) {
			putchar(i + '0');
			i++;
		}
	while
		(p <= 'f') {
			putchar(p);
			p++;
		}
	putchar('\n');
	return (0);
}
