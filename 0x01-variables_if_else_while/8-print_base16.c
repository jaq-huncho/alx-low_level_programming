#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int p;

	char i;

	p = 10;
	i = 'a';
	while
		(p < 10) {
			putchar(p + '0');
			p++;
		}
	while
		(i <= 'f') {
			putchar(i);
			i++;
		}
	return (0);
}
