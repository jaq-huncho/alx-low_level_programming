#include "main.h"

/**
 *_isupper - checks if a charcter is uppercase
 *@c:charcter is to be tested
 *return: 1whether it is, 0 otherwise
 */

int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'a')
			{
			return (1);
			}
			return (0);
			}
